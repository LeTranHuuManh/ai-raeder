name: ai_reader
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  flutter_svg: ^2.2.1
  provider: ^6.1.5+1
  get: ^4.7.2
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_storage: ^13.0.3
  google_sign_in: ^6.2.1
  # flutter_facebook_auth: ^7.1.2  # Removed: Requires Facebook SDK initialization
  # vocsy_epub_viewer: ^3.0.0  # Removed: Not compatible with Flutter 3.9+
  # flutter_pdfview: ^1.4.3  # Removed: Not compatible with web
  pdfx: ^2.7.0 # Cross-platform PDF viewer with lazy loading
  file_picker: ^10.3.3
  http: ^1.5.0
  dio: ^5.9.0
  flutter_tts: ^4.2.3
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  flutter_rating_bar: ^4.0.1
  intl: ^0.20.2
  uuid: ^4.5.1
  flutter_dotenv: ^6.0.0
  crypto: ^3.0.3
  audioplayers: ^6.1.0
  googleapis_auth: ^1.5.0
  syncfusion_flutter_pdf: ^28.1.36

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.26.2

flutter:
  uses-material-design: true

  # Assets for .env file (required for web)
  assets:
    - assets/.env
